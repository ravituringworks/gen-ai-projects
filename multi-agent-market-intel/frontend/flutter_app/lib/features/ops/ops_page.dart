import 'package:flutter/material.dart';
class OpsPage extends StatelessWidget{ const OpsPage({super.key});
  @override Widget build(BuildContext context){
    return const Center(child: Text('Ops & Compliance — approvals, audit trail (TODO)'));
  }
}
