import 'package:flutter/material.dart';
class AnalystWorkbenchPage extends StatelessWidget{ const AnalystWorkbenchPage({super.key});
  @override Widget build(BuildContext context){
    return const Center(child: Text('Analyst Workbench — feature/label explorer (TODO)'));
  }
}
